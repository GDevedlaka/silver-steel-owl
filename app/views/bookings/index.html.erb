<h1> Your Bookings </h1>
<div class="bookings">
<h1><center> You have <%=@bookings.size %> Bookings </center></h1>
</div>

<div class="container">
  <div class="row">
    <% @bookings.each do |s| %>
       <div class="card">
          <ul>
          <li><%= s.email %></li>
          <li>£<%= s.price %></li>
          <li> <%= s.status %></li>
       </div>
       <% end %>
  </div>
</div>

